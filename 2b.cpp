#include <string.h>
#include <stdio.h>

int main() {

    char cadena[] = "No me mandes a ordinaria";

    int longitud = strlen(cadena);

    printf("La longitud de '%s' es: %d\n", cadena, longitud);
